import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const brand = Color(0xFF4F46E5);
  static const brandDark = Color(0xFF312E81);
  static const brandContainer = Color(0xFFE9E7FF);
  static const purple = Color(0xFF7C3AED);
  static const teal = Color(0xFF0F766E);
  static const tealContainer = Color(0xFFCCFBF1);
  static const warm = Color(0xFFEA7C19);
  static const warmContainer = Color(0xFFFFEDD5);
  static const success = Color(0xFF15803D);
  static const warning = Color(0xFFB45309);
  static const error = Color(0xFFB42318);
  static const ink = Color(0xFF171A21);
  static const mutedInk = Color(0xFF5B6170);
  static const canvas = Color(0xFFF6F7FC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceTinted = Color(0xFFF0F1FA);
  static const outline = Color(0xFFDDE1EA);
}

class AppSpacing {
  const AppSpacing._();

  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 12.0;
  static const lg = 16.0;
  static const xl = 20.0;
  static const xxl = 24.0;
  static const section = 32.0;
}

class AppRadii {
  const AppRadii._();

  static const control = 12.0;
  static const card = 16.0;
  static const feature = 20.0;
}
